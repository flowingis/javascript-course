const anagrams = require('./code/anagrams');

describe('anagrams', () => {
    /*
        Dovete qui scrivere tutti i possibili test di non regressione per il codice che abbiamo analizzato insieme
     */
});